# Versions per ../../PINS.md ("serving service, weeks 2, tier 0 CPU path").
# ON COLAB: delete the torch line rather than pinning it. Colab ships torch
# preinstalled and installing a second one breaks the runtime. Pin it locally.

fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.9.*
transformers==4.46.*
torch==2.5.*
accelerate==1.1.*
openai==1.54.*
httpx==0.27.*
